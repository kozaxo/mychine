[defaults]
inventory = inventory.ini
host_key_checking = False
interpreter_python = auto_silent
retry_files_enabled = False
# Without this, become (sudo) password relay can fail with "Timed out
# waiting for become success" on some ansible-core/sudo combinations —
# pipelining executes the module in fewer separate exec/stdin round-trips.
pipelining = True
